# Prod overrides. Pair with values-prod.secrets.yaml (gitignored).
global:
  namespace: recruiter-report-prod

image:
  tag: prod

host: recruiter-report-api.dc-forte.com

tls:
  secretName: recruiter-report-tls

replicas: 1

env:
  ENVIRONMENT: production
