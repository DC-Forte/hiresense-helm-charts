# Staging overrides. Pair with values-staging.secrets.yaml (gitignored).
global:
  namespace: interviewhandoff-staging

image:
  tag: staging

worker:
  image:
    tag: staging

replicas: 1

env:
  APP_ENV: staging
  MATCHENGINE_URL: "http://matchengine.matchengine-staging.svc.cluster.local"
